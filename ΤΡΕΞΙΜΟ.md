# Πώς να τρέξεις την εφαρμογή και να τη δεις στο browser (UI/UX)

## Προαπαιτούμενα (μία φορά)

1. **MySQL**: Βάση `inventory_app` δημιουργημένη. Άνοιξε `backend/src/main/resources/application.properties` και βάλε το password του MySQL.
2. **Frontend dependencies**: Από ρίζα project τρέξε:
   ```bash
   npm run install:frontend
   ```

---

## Τρέξιμο (για να δεις το UI)

### Βήμα 1 — Backend (πρώτο terminal)

```powershell
cd backend
.\mvnw.cmd spring-boot:run
```
(PowerShell απαιτεί `.\` μπροστά από το script.)

Περίμενε μέχρι να δεις `Started InventoryLogicApplication`. Το API τρέχει στο **http://localhost:8080**.

### Βήμα 2 — Frontend (δεύτερο terminal)

Άνοιξε **νέο** terminal, από τη ρίζα του project:

```bash
cd C:\Users\anast\IdeaProjects\InventoryLogic
npm run dev
```

Θα ανοίξει το Vite dev server. Στο terminal θα εμφανιστεί κάτι σαν:

```
  ➜  Local:   http://localhost:5173/
```

### Βήμα 3 — Browser

Άνοιξε το browser και πήγαινε στο:

**http://localhost:5173**

Εκεί θα δεις το UI: λίστα προϊόντων, αναζήτηση, κουμπιά «Νέο προϊόν», «Επεξεργασία», «Ιστορικό» και τα κουμπιά ±1 / ±5 / ±10 για αλλαγή ποσότητας.

---

## Σύνοψη

| Τι τρέχει    | Port | URL                    |
|-------------|------|------------------------|
| Backend API | 8080 | http://localhost:8080  |
| Swagger     | 8080 | http://localhost:8080/swagger-ui.html |
| Frontend UI | 5173 | **http://localhost:5173** ← άνοιξε αυτό για UI |

**Πότε είναι έτοιμο:** Μόλις τρέχουν και τα δύο (backend + `npm run dev`) και ανοίξεις το **http://localhost:5173** στο browser.
