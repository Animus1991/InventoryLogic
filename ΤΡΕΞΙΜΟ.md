# Πώς να τρέξεις την εφαρμογή και να τη δεις στο browser (UI/UX)

## Προαπαιτούμενα (μία φορά)

1. **MySQL**: Βάση `inventory_app` δημιουργημένη. Άνοιξε `backend/src/main/resources/application.properties` και βάλε το password του MySQL.
2. **Frontend dependencies**: Από ρίζα project:
   ```bash
   npm run install:frontend
   ```

---

## Τρέξιμο (για να δεις το UI)

### Βήμα 1 — Backend (πρώτο terminal)

**Σημαντικό:** Χρησιμοποίησε `.\mvnw.cmd` (με τελεία και ανάποδη κάθετο) στο Windows.

```powershell
cd backend
.\mvnw.cmd spring-boot:run
```

Περίμενε μέχρι να δεις `Started InventoryLogicApplication`. Το API τρέχει στο **http://localhost:8081**.

Αν εμφανιστεί **404 στο /api/auth/login**, σταμάτα το backend (Ctrl+C) και τρέξε ξανά ώστε να φορτώσουν τα endpoints σύνδεσης/εγγραφής.

### Βήμα 2 — Frontend (δεύτερο terminal)

Από τη ρίζα του project:

```bash
npm run dev
```

Θα ανοίξει το Vite dev server. Στο terminal θα εμφανιστεί κάτι σαν:

```
  ➜  Local:   http://localhost:5173/
```

### Βήμα 3 — Browser ή κινητό

Άνοιξε στο browser:

**http://localhost:5173**

Για δοκιμή από **κινητό** στο ίδιο δίκτυο: χρησιμοποίησε το Network URL που εμφανίζει το Vite (π.χ. http://192.168.x.x:5173) και βεβαιώσου ότι το backend CORS επιτρέπει αυτή τη διεύθυνση (`app.cors.allowed-origins` στο `application.properties`).

Εκεί θα δεις: Κεντρική οθόνη (Dashboard), Προϊόντα, Αναφορές, Τι να παραγγείλω, σάρωση/πληκτρολόγηση barcode, σύνδεση/εγγραφή.

---

## Σύνοψη

| Τι τρέχει    | Port | URL                    |
|-------------|------|------------------------|
| Backend API | **8081** | http://localhost:8081  |
| Swagger     | 8081 | http://localhost:8081/swagger-ui.html |
| Frontend UI | 5173 | **http://localhost:5173** ← άνοιξε αυτό για UI |

**Πότε είναι έτοιμο:** Μόλις τρέχουν backend + `npm run dev` και ανοίξεις το **http://localhost:5173** στο browser.

**PWA:** Μετά το build (`npm run build`) και αν σερβίρεται το frontend μέσω HTTPS, οι χρήστες μπορούν να εγκαταστήσουν την εφαρμογή ως εφαρμογή (Add to Home Screen) για χρήση από κινητό.
